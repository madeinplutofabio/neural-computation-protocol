[package]
name = "ncp-echo"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "NCP reference echo Brick — returns input as output"

[lib]
crate-type = ["cdylib"]

[dependencies]
minicbor = { version = "0.25", default-features = false, features = ["alloc"] }

[features]
trap = []
