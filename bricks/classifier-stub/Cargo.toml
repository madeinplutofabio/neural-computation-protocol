[package]
name = "ncp-classifier-stub"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "NCP stub classifier — keyword-based sentiment routing for benchmarks"

[lib]
crate-type = ["cdylib"]

[dependencies]
minicbor = { version = "0.25", default-features = false, features = ["alloc"] }
