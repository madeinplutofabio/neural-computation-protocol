{
  "name": "@ncp/validate",
  "version": "0.1.0",
  "description": "NCP manifest validator — structural (JSON Schema) + cross-field invariant validation for Brick and Graph manifests.",
  "type": "module",
  "bin": {
    "ncp-validate": "./dist/cli.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate-examples": "node dist/cli.js brick ../../examples/bricks/sentiment-gate/manifest.yaml && node dist/cli.js brick ../../examples/bricks/llm-escalation/manifest.yaml && node dist/cli.js graph ../../examples/graphs/support-routing/graph.yaml"
  },
  "dependencies": {
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.0",
    "yaml": "^2.6.0",
    "commander": "^12.0.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "Apache-2.0"
}
